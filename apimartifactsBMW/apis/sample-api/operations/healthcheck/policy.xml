<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
	<!-- Throttle, authorize, validate, cache, or transform the requests -->
	<inbound>
		<base />
		<trace source="request-info" severity="information">
            客户端调用IP：@{context.Request.IpAddress}
            请求方式：@{context.Request.Method} | 完整请求地址：@{context.Request.Url}
            自定义请求头-用户ID：@{context.Request.Headers.GetValueOrDefault("userId", "未传用户ID")}
            自定义请求头-令牌：@{context.Request.Headers.GetValueOrDefault("accessToken", "未传令牌")}
            接口请求体内容：@{context.Request.Body.As&lt;string&gt;()}
        </trace>
		<return-response>
			<set-status code="200" reason="OK" />
			<set-header name="Content-Type" exists-action="override">
				<value>application/json</value>
			</set-header>
			<set-body>@{                  var jsonResponse = new JObject();                  jsonResponse["status"] = "healthy";                  jsonResponse["timestamp"] = DateTime.UtcNow.ToString("o");                  return jsonResponse.ToString();              }</set-body>
		</return-response>
	</inbound>
	<!-- Control if and how the requests are forwarded to services  -->
	<backend>
		<base />
	</backend>
	<!-- Customize the responses -->
	<outbound>
		<base />
	</outbound>
	<!-- Handle exceptions and customize error responses  -->
	<on-error>
		<base />
	</on-error>
</policies>